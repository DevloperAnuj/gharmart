import 'package:flutter/material.dart';

class PropertyImagesCarousel extends StatelessWidget {
  const PropertyImagesCarousel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
